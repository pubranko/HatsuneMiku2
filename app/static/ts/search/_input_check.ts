/** 画面の入力内容をチェックする。エラーがあった場合、error_flg
 * @param 検索グループ・検索条件を解析して配列にしたデータ(input_list)
 */
export const input_check = (input_list: Array<any>): { [key: string]: boolean } => {

    //とりあえず後回し

    return { "error_flg": false }
}
